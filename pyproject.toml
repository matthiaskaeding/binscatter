[project]
name = "binscatter"
version = "0.1.0"
description = "Creating binscatter plots"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2.3",
    "polars>=1.27.1",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
